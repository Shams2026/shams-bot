import requests
from datetime import datetime

# بيانات بوت Shams المحدثة
TOKEN = "8305317876:AAHkPFr8idftz1Rgc6-tdCugaaZTrQsZnP4"
CHAT_ID = "-5216371452"

def send_distraction_alert():
    # جلب الوقت والتاريخ الحاليين [2026-02-05]
    now = datetime.now()
    current_time = now.strftime("%I:%M %p")
    current_date = now.strftime("%Y-%m-%d")

    # تنسيق الرسالة المطلوب مع استبدال "كومنت" بـ "الاسم" [2026-01-30]
    message = (
        f"⚠️ *تنبيه تشتت انتباه - Shams*\n\n"
        f"👤 الاسم: [أدخل اسم الطالب]\n"
        f"📅 التاريخ: {current_date}\n"
        f"⏰ الوقت: {current_time}\n\n"
        f"🚫 *ملاحظة:* تم رصد الطالب وهو يفتح تطبيق **YouTube** الآن على الجهاز أثناء وقت السكشن (3 - 5)."
    )

    url = f"https://api.telegram.org/bot{TOKEN}/sendMessage"
    payload = {
        "chat_id": CHAT_ID,
        "text": message,
        "parse_mode": "Markdown"
    }
    
    response = requests.post(url, json=payload)
    if response.status_code == 200:
        print("✅ تم إرسال تنبيه اليوتيوب بنجاح")
    else:
        print(f"❌ فشل الإرسال: {response.text}")

if __name__ == "__main__":
    send_distraction_alert()
