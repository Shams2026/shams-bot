name: "2- Shams: YouTube Alert" # هذا الاسم الذي سيظهر تحته
on:
  workflow_dispatch:
jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install requests
      - run: python bot.py
